<h2><%= @ticket.title %></h2>
<%= simple_format(@ticket.description) %>
<menu>
  <%= link_to "Edit", [:edit, @project, @ticket] %>
</menu>
<menu>
  <%= link_to "Delete", [@project, @ticket], :method => :delete, :confirm => 'Are you sure you want to delete this ticket?' %>
</menu>
